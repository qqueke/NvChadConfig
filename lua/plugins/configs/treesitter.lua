local options = {
  --  ensure_installed = { "lua", "vim", "vimdoc", "c", "cpp", "javascript", "go", "cuda", "rust", "python" },
  auto_install = true,
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
